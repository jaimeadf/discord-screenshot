enum ResourceState {
    MISSING = 'missing',
    UNINITIALIZED = 'uninitialized',
    STOPPING = 'stopping',
    STOPPED = 'stopped',
    STARTING = 'starting',
    STARTED = 'started',
    UNKNOWN = 'unknown'
}

export default ResourceState;
